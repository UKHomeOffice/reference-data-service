---
pipeline:

  synch_dev_secrets:
    image: quay.io/ukhomeofficedigital/cop-secrets
    environment:
      - DRONE_SERVER=https://drone.acp.homeoffice.gov.uk
      - DEPLOY_ENV=dev
    secrets:
      - source: DEV_DRONE_AWS_ACCESS_KEY_ID
        target: AWS_ACCESS_KEY_ID
      - source: DEV_DRONE_AWS_SECRET_ACCESS_KEY
        target: AWS_SECRET_ACCESS_KEY
      - source: DRONE_PUBLIC_TOKEN
        target: DRONE_TOKEN
    when:
      environment: secrets
      event: deployment

  synch_staging_secrets:
    image: quay.io/ukhomeofficedigital/cop-secrets
    environment:
      - DRONE_SERVER=https://drone.acp.homeoffice.gov.uk
      - DEPLOY_ENV=staging
    secrets:
      - source: STAGING_DRONE_AWS_ACCESS_KEY_ID
        target: AWS_ACCESS_KEY_ID
      - source: STAGING_DRONE_AWS_SECRET_ACCESS_KEY
        target: AWS_SECRET_ACCESS_KEY
      - source: DRONE_PUBLIC_TOKEN
        target: DRONE_TOKEN
    when:
      environment: secrets
      event: deployment

  synch_production_secrets:
    image: quay.io/ukhomeofficedigital/cop-secrets
    environment:
      - DRONE_SERVER=https://drone.acp.homeoffice.gov.uk
      - DEPLOY_ENV=production
    secrets:
      - source: PRODUCTION_DRONE_AWS_ACCESS_KEY_ID
        target: AWS_ACCESS_KEY_ID
      - source: PRODUCTION_DRONE_AWS_SECRET_ACCESS_KEY
        target: AWS_SECRET_ACCESS_KEY
      - source: DRONE_PUBLIC_TOKEN
        target: DRONE_TOKEN
    when:
      environment: secrets
      event: deployment

  deploy_to_dev:
    image: quay.io/ukhomeofficedigital/kd
    secrets:
      - source: DEV_REFERENCE_DATA_SERVICE_AWS_SECRET_MANAGER_ENABLED
        target: REFERENCE_DATA_SERVICE_AWS_SECRET_MANAGER_ENABLED
      - source: AWS_REGION
        target: REFERENCE_DATA_SERVICE_AWS_REGION
      - source: DEV_REFERENCE_DATA_SERVICE_AWS_ACCESS_KEY_ID
        target: REFERENCE_DATA_SERVICE_AWS_ACCESS_KEY_ID
      - source: DEV_REFERENCE_DATA_SERVICE_AWS_SECRET_ACCESS_KEY
        target: REFERENCE_DATA_SERVICE_AWS_SECRET_ACCESS_KEY
      - source: PROTOCOL_HTTPS
        target: API_REF_PROTOCOL
      - source: DEV_API_REF_URL
        target: API_REF_URL
      - source: DEV_ENVIRONMENT
        target: ENVIRONMENT
      - source: PROTOCOL_HTTPS
        target: KEYCLOAK_PROTOCOL
      - source: DEV_KEYCLOAK_REALM
        target: KEYCLOAK_REALM
      - source: DEV_KEYCLOAK_URL
        target: KEYCLOAK_URL
      - source: DEV_KUBE_SERVER
        target: KUBE_SERVER
      - source: DEV_API_REF_KUBE_TOKEN
        target: KUBE_TOKEN
      - source: WWW_REF_IMAGE
        target: WWW_REF_IMAGE
      - source: WWW_REF_KEYCLOAK_CLIENT_ID
        target: KEYCLOAK_CLIENT_ID
      - source: DEV_KUBE_NAMESPACE_REFDATA
        target: KUBE_NAMESPACE
      - source: WWW_REF_NAME
        target: WWW_REF_NAME
      - source: WWW_REF_PORT
        target: WWW_REF_PORT
      - source: PROTOCOL_HTTPS
        target: WWW_REF_PROTOCOL
      - source: DEV_WWW_REF_READ_ONLY_MODE
        target: WWW_REF_READ_ONLY_MODE
      - source: DEV_WWW_REF_URL
        target: WWW_REF_URL
    commands:
      - export REFERENCE_DATA_SERVICE_NAME="reference-data-service"
      - export REFERENCE_DATA_SERVICE_TAG=$${DRONE_COMMIT_SHA}
      - export REFERENCE_DATA_SERVICE_SPRING_PROFILE_ACTIVE="dev"
      - export WWW_REF_TAG=$${DRONE_COMMIT_SHA}
      - kd --insecure-skip-tls-verify -f kube/cert.yml
      - kd --insecure-skip-tls-verify -f kube/secret.yml
      - kd --insecure-skip-tls-verify -f kube/network-policy.yml
      - kd --insecure-skip-tls-verify -f kube/service.yml
      - kd --insecure-skip-tls-verify -f kube/deployment.yml
      - kd --insecure-skip-tls-verify -f kube/ingress.yml
    when:
      event: deployment
      environment: dev
      branch: master

  deploy_to_staging:
    image: quay.io/ukhomeofficedigital/kd
    secrets:
      - source: STAGING_REFERENCE_DATA_SERVICE_AWS_SECRET_MANAGER_ENABLED
        target: REFERENCE_DATA_SERVICE_AWS_SECRET_MANAGER_ENABLED
      - source: AWS_REGION
        target: REFERENCE_DATA_SERVICE_AWS_REGION
      - source: STAGING_REFERENCE_DATA_SERVICE_AWS_ACCESS_KEY_ID
        target: REFERENCE_DATA_SERVICE_AWS_ACCESS_KEY_ID
      - source: STAGING_REFERENCE_DATA_SERVICE_AWS_SECRET_ACCESS_KEY
        target: REFERENCE_DATA_SERVICE_AWS_SECRET_ACCESS_KEY
      - source: PROTOCOL_HTTPS
        target: API_REF_PROTOCOL
      - source: STAGING_API_REF_URL
        target: API_REF_URL
      - source: STAGING_ENVIRONMENT
        target: ENVIRONMENT
      - source: PROTOCOL_HTTPS
        target: KEYCLOAK_PROTOCOL
      - source: STAGING_KEYCLOAK_REALM
        target: KEYCLOAK_REALM
      - source: STAGING_KEYCLOAK_URL
        target: KEYCLOAK_URL
      - source: STAGING_KUBE_SERVER
        target: KUBE_SERVER
      - source: STAGING_API_REF_KUBE_TOKEN
        target: KUBE_TOKEN
      - source: WWW_REF_IMAGE
        target: WWW_REF_IMAGE
      - source: WWW_REF_KEYCLOAK_CLIENT_ID
        target: KEYCLOAK_CLIENT_ID
      - source: STAGING_KUBE_NAMESPACE_REFDATA
        target: KUBE_NAMESPACE
      - source: WWW_REF_NAME
        target: WWW_REF_NAME
      - source: WWW_REF_PORT
        target: WWW_REF_PORT
      - source: PROTOCOL_HTTPS
        target: WWW_REF_PROTOCOL
      - source: STAGING_WWW_REF_READ_ONLY_MODE
        target: WWW_REF_READ_ONLY_MODE
      - source: STAGING_WWW_REF_URL
        target: WWW_REF_URL
    commands:
      - export REFERENCE_DATA_SERVICE_NAME="reference-data-service"
      - export REFERENCE_DATA_SERVICE_TAG=$${DRONE_COMMIT_SHA}
      - export REFERENCE_DATA_SERVICE_SPRING_PROFILE_ACTIVE="staging"
      - export WWW_REF_TAG=$${DRONE_COMMIT_SHA}
      - kd --insecure-skip-tls-verify -f kube/cert.yml
      - kd --insecure-skip-tls-verify -f kube/secret.yml
      - kd --insecure-skip-tls-verify -f kube/network-policy.yml
      - kd --insecure-skip-tls-verify -f kube/service.yml
      - kd --insecure-skip-tls-verify -f kube/deployment.yml
      - kd --insecure-skip-tls-verify -f kube/ingress.yml
    when:
      branch: master
      event: deployment
      environment: staging

  deploy_to_production:
    image: quay.io/ukhomeofficedigital/kd
    secrets:
      - source: PRODUCTION_REFERENCE_DATA_SERVICE_AWS_SECRET_MANAGER_ENABLED
        target: REFERENCE_DATA_SERVICE_AWS_SECRET_MANAGER_ENABLED
      - source: AWS_REGION
        target: REFERENCE_DATA_SERVICE_AWS_REGION
      - source: PRODUCTION_REFERENCE_DATA_SERVICE_AWS_ACCESS_KEY_ID
        target: REFERENCE_DATA_SERVICE_AWS_ACCESS_KEY_ID
      - source: PRODUCTION_REFERENCE_DATA_SERVICE_AWS_SECRET_ACCESS_KEY
        target: REFERENCE_DATA_SERVICE_AWS_SECRET_ACCESS_KEY
      - source: PROTOCOL_HTTPS
        target: API_REF_PROTOCOL
      - source: PRODUCTION_API_REF_URL
        target: API_REF_URL
      - source: PRODUCTION_ENVIRONMENT
        target: ENVIRONMENT
      - source: PROTOCOL_HTTPS
        target: KEYCLOAK_PROTOCOL
      - source: PRODUCTION_KEYCLOAK_REALM
        target: KEYCLOAK_REALM
      - source: PRODUCTION_KEYCLOAK_URL
        target: KEYCLOAK_URL
      - source: PRODUCTION_KUBE_SERVER
        target: KUBE_SERVER
      - source: PRODUCTION_API_REF_KUBE_TOKEN
        target: KUBE_TOKEN
      - source: WWW_REF_IMAGE
        target: WWW_REF_IMAGE
      - source: WWW_REF_KEYCLOAK_CLIENT_ID
        target: KEYCLOAK_CLIENT_ID
      - source: PRODUCTION_KUBE_NAMESPACE_REFDATA
        target: KUBE_NAMESPACE
      - source: WWW_REF_NAME
        target: WWW_REF_NAME
      - source: WWW_REF_PORT
        target: WWW_REF_PORT
      - source: PROTOCOL_HTTPS
        target: WWW_REF_PROTOCOL
      - source: PRODUCTION_WWW_REF_READ_ONLY_MODE
        target: WWW_REF_READ_ONLY_MODE
      - source: PRODUCTION_WWW_REF_URL
        target: WWW_REF_URL
    commands:
      - export WWW_REF_TAG=$${DRONE_COMMIT_SHA}
      - export REFERENCE_DATA_SERVICE_NAME="reference-data-service"
      - export REFERENCE_DATA_SERVICE_TAG=$${DRONE_COMMIT_SHA}
      - export REFERENCE_DATA_SERVICE_SPRING_PROFILE_ACTIVE="production"
      - kd --insecure-skip-tls-verify -f kube/cert.yml
      - kd --insecure-skip-tls-verify -f kube/secret.yml
      - kd --insecure-skip-tls-verify -f kube/network-policy.yml
      - kd --insecure-skip-tls-verify -f kube/service.yml
      - kd --insecure-skip-tls-verify -f kube/deployment.yml
      - kd --insecure-skip-tls-verify -f kube/ingress.yml
    when:
      branch: master
      event: deployment
      environment: production

  notify:
    image: plugins/slack
    secrets: [ SLACK_WEBHOOK ]
    channel: cop-deployments
    username: Drone Build Watcher
    template: >
      {{#build.deployTo}}
        *{{repo.name}} - Build {{build.number}} - {{uppercasefirst build.deployTo}} - {{uppercase build.status}}*
        {{build.link}}
      {{else}}
        *{{repo.name}} - Build {{build.number}} - Development - {{uppercase build.status}}*
        {{build.link}}
      {{/build.deployTo}}
    when:
      branch: master
      event: [ push, deployment ]
      status: [ success, failure ]
